{
  "config": {
    "step": {
      "user": {
        "title": "Neo Dashboard Tools",
        "description": "Stores Neo Dashboard modules as files on the server. Manage them in your dashboard (Neo Card → Extensions)."
      }
    },
    "abort": {
      "already_configured": "Neo Dashboard Tools is already configured."
    }
  }
}
