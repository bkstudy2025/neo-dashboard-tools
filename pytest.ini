[pytest]
asyncio_mode = auto
testpaths = tests
; Repo-Root in den Modulpfad: die Tests importieren custom_components.…
; (nötig, wenn pytest direkt statt über "python -m pytest" gestartet wird)
pythonpath = .
